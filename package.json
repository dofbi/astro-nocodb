{
  "name": "astro-nocodb-monorepo",
  "private": true,
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "publish": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}